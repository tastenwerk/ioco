ioco.modal(<%- JSON.stringify(partial) %>, {
  complete: function( modal ){
    $('.js-get-focus').focus();
    var parent = $("#ioco-folders").data("kendoTreeView").dataItem('.k-state-selected')
    if( parent ){
      $(modal).find('.label.parent').text(parent.name);
      $(modal).find('input.parent').val(parent._id+':'+parent._type);
      $(modal).find('.parent').show();
    }
  }
})