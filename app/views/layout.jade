!!! 5
html
  head
    include header
    block header
  body
    input( type='hidden', id='_csrf', value=_csrf )
    input( type='hidden', id='_host_master', value=ioco.config.hostname )
    input( type='hidden', id='_fallbackLocale', value=ioco.config.site.fallbackLocale )

    block content